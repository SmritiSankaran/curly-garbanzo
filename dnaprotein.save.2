 print(x)
                if (len(parts)- 1) == 1:
                        d[parts[0]] = parts[1]
                if (len(parts) - 1) == 2:
                        d[parts[0]] = parts[1],parts[2]
print df = open("/share/data/genetic-code.txt")
lines = f.readlines()
for line in lines:
	y = line.split()
	for x in y:
		print(x)
		d[
